# ============================================================
# 构建产物
# ------------------------------------------------------------
# build/ 由 cmake 本地生成，不需要纳入版本管理
# （重新配置：cmake --preset Debug && cmake --build build/Debug）
# ============================================================
build/

# ============================================================
# 工具 / 临时产物
# ============================================================
.cowork-temp/
.merkle-snapshot.json
