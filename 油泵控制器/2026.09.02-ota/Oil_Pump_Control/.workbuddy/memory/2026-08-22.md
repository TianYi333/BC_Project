# 2026-08-22 工作日志

## OLED 调试菜单状态机调整（UserCode/Logic/oled_page.c）
- 问题：进入调试二级菜单时 `debug_mode_callback` 无条件强制 `SYS_STATE_SETTING`，导致 `debug_pid_tune_callback`（PID 在线整定）进入时已被切到设置模式，PID 闭环被旁路、无法实时整定（"LIVE"遥测恒为 OFF）。
- 修改：
  1. 删除 `debug_mode_callback` 进入时的全局强制 SETTING（改为注释说明，由各子页自决）。
  2. 删除 `debug_io_monitor_callback`、`debug_adc_watch_callback` 各自的强制 SETTING（二者为只读监视页，现可边运行边看实时值）。
  3. 保留 `debug_manual_ctrl_callback` 与 `debug_modbus_test_callback` 的强制 SETTING（用户指定仅这两页需要暂停泵）。
- 现状：调试子页中仅 Manual Ctrl / Modbus Test 强制 SETTING；PID Tune 保持进入前状态（PRESS/HOLD 时实时生效）；IO Monitor / ADC Watch 不切模式。
- 注意：未编译验证（本环境缺 arm-none-eabi 工具链 + 完整 Cube 库），需本机 STM32CubeIDE build 确认。

## 之前累计改动（2026-08-21）
- net_comm_task.c：Bug① 分片 pbuf 接收修正、Bug② 互斥锁漏释放补全。
- Core/Src/sysmem.c：newlib malloc 锁改为中断安全临界段实现（`xPortIsInsideInterrupt` 分支用 FROM_ISR 临界段，任务上下文用互斥锁）。
- Core/Src/freertos.c：IWDGTask 优先级抬到 osPriorityRealtime7(=55)。
- LWIP/Target/lwipopts.h：恢复 CubeMX 默认 malloc 模式（静态池会撑爆 RAM_D1）。
- 设计约定：网络重放防护（Bug④）是用户有意关闭，非缺陷，不修复。

## 泄压阀 valve_state → 实际 GPIO 驱动（UserCode）
- 用户确认：泄压阀接 `DO_OUTPUT_0`(PE3)，开阀=高电平(valve_state=1→GPIO_PIN_SET)。
- 新增 `Valve_Drive(uint8_t open)`（main_logic.c 定义，main_logic.h 声明）：`HAL_GPIO_WritePin(DO_OUTPUT_0_GPIO_Port, DO_OUTPUT_0_Pin, open?SET:RESET)`。
- 在所有写 `valve_state` 的位置同步调用 Valve_Drive：
  - main_logic.c：SysStatus_Init(valve=0)、STOP→关(472)、RELEASE→开(648)、RELEASE泄压完→关(675)、严重超压→target(802)
  - oled_page.c：debug_manual_ctrl 阀门短按切换(1859)
  - syncif.c：Modbus 保持寄存器 201 远程阀门(286)
  - net_comm_task.c：remote_control 命令 valve_state(2180)
- 屏蔽电机 RDY 读取：step_motor.c `step_motor_read_rdy()` 改为恒返回 1U（PE3 现为阀门输出，不再作电机 RDY 输入，避免误报）。step_motor.h 中 RDY_GPIO 宏保留未用。
- 注意：PE3 在 gpio.c 已是 `GPIO_MODE_OUTPUT_PP`（无需改 GPIO 模式）。DO_OUTPUT_3(PE6) 实际未配置为输出、亦未使用。
- 读取侧：各显示/上报仍读 `valve_state` 变量（输出脚电平回读无额外意义），未改。

## 泄压阀一致性校验（读实际引脚 vs 命令意图）
- 新增 `Valve_ReadState(void)`（main_logic.c）：`HAL_GPIO_ReadPin(PE3)==SET?1:0`，回读实际引脚电平。
- `_SYS_STATUS` 新增 `valve_hw_state`（实际引脚电平）；`main_logic.h` 加 `VALVE_MISMATCH_DEBOUNCE 2U`。
- `MainLogicTask`（每200ms一轮）一致性校验：`actual=Valve_ReadState()`，与 `valve_state`(命令意图) 比对，
  防抖 `valve_mismatch_cnt>=2` 后若仍不一致 → `sys_warning=1`（阀门一致性失配告警）；一致则清零。
  结果写入快照：`valve_hw_state=actual`、`sys_warning=calc_valve_mismatch`（有变化才写，避免锁抖动）。
- SysStatus_Init 初始化 `valve_hw_state=0`。
- 外部上报：
  - syncif.c：Modbus 保持寄存器 206=valve_hw_state、207=sys_warning（REG_Holding_NREGS=500，安全）。
  - OLED 调试手动页：y=84 显示 `HW Valve:OPEN/CLOSE`，`sys_warning!=0` 时标 `MISMATCH`。
  - 网络 JSON 心跳/状态报文**未加新 key**（避免破坏 HMAC-SHA256 签名信封兼容性）；失配已通过既有 `sys_warning` 字段上报。
- 局限（已告知用户）：读输出引脚仅反映 MCU 输出寄存器状态，可捕获“变量与引脚失步”，但**无法检测阀门机械卡死/线圈烧毁**（需独立阀位反馈开关接输入脚）。
- 未编译验证（缺 arm-none-eabi 工具链 + 完整 Cube 库），需本机 build 确认。

## 【撤销】泄压阀一致性校验（2026-08-22 后续）
- 用户决定不加“读实际引脚做阀门一致性校验”，恢复添加前状态。
- 已删除：Valve_ReadState()、_SYS_STATUS.valve_hw_state、VALVE_MISMATCH_DEBOUNCE；
  MainLogicTask 中的一致性校验块（actual/calc_valve_mismatch/valve_mismatch_cnt 全部移除）；
  syncif.c Modbus 0206；oled_page.c 调试手动页 `HW Valve:`/`MISMATCH` 显示。
- 保留：泄压阀 GPIO 驱动（Valve_Drive + PE3）及其在状态机/Manual Ctrl/Modbus 201/remote_control 的写入（属上一节“GPIO 驱动”特性，非本次一致性校验）。
- sys_warning 是用户原有字段（结构体/初始化/心跳结构/网络JSON 5处/Modbus 0207），**保留不动**。一致性校验只是复用了它（快照写入 `sys_warning=calc_valve_mismatch`），该赋值随一致性块一并删除，sys_warning 恢复为仅声明+上报、恒为 0 的原始状态。
- 未编译验证，需本机 STM32CubeIDE build 确认。

## 【纠正】sys_warning 误删并已恢复（2026-08-22 后续）
- 误判：上一轮"完全还原"时把用户原有的 `sys_warning` 字段连同其所有引用（main_logic.h 结构体、SysStatus_Init、net_comm_task.h 心跳结构、net_comm_task.c 5处网络上报、syncif.c Modbus 0207）一并删除。
- 用户纠正：只应撤销"读引脚做阀门一致性校验"的修改，sys_warning 是原有字段，不该删。
- 已恢复：sys_warning 全部 9 处（结构体/初始化/心跳结构/5处网络上报/Modbus 0207）重新加回；一致性校验残留（valve_hw_state/Valve_ReadState/VALVE_MISMATCH_DEBOUNCE/valve_mismatch_cnt/calc_valve_mismatch/actual_valve/OLED HW Valve:MISMATCH/Modbus 0206）保持删除。
- 结论：最终状态 = 泄压阀 GPIO 驱动(Valve_Drive+PE3) 保留 + sys_warning 原有字段/上报保留 + 一致性校验整套移除。此即"仅撤销一致性校验"的正确状态。
- 未编译验证，需本机 STM32CubeIDE build 确认。

## PID 整定页 LIVE/OFF 标签改实时状态（2026-08-22 后续）
- 问题：`debug_pid_tune_callback` 底部 `LIVE/OFF` 标记用进页面时冻结的 `enter_snap.sys_start` 判定，系统状态切换后标签不更新（误导）。
- 修改（UserCode/Logic/oled_page.c）：删除无用的 `enter_snap` 声明与读取（仅此标签用到，删后避免未用变量告警）；`loop_tag` 改为读每帧实时刷新的 `sys_tmp.sys_start`（sys_tmp 在 while 顶部每轮 SysStatus_ReadSnapshot）。
- 现状：标签随系统状态实时变化（PRESS/HOLD→LIVE，其余→OFF）。仍不强制 SETTING，PID 闭环在跑时调参即时可见。
- 未编译验证，需本机 STM32CubeIDE build 确认。

## PID 整定页交互对齐 debug_manual_ctrl_callback（2026-08-22 后续）
- 需求：整定页交互参考手动控制页——旋转选择 Kp/Ki/Kd，短按进入编辑（*标记），短按保存并退出编辑回选择态。
- 修改（UserCode/Logic/oled_page.c `debug_pid_tune_callback`）：
  - 新增 `edit_mode`（0浏览/1编辑）；显示：浏览选中行 `>`，编辑选中行 `*`。
  - 浏览模式：旋转 CW/CCW 仅在 Kp/Ki/Kd 间切换 sel_idx（不再直接改值）；短按 ENCODER_PUTH 进入编辑。
  - 编辑模式：旋转改选中参数值，并即时写入 sys_cfg（闭环立即生效，保留“调参即时可见”）；短按 ENCODER_PUTH 保存(sys_cfg)+退出编辑回选择态。
  - 长按 ENCODER_PUTH_LONG：写 sys_cfg + sync_to_sysdb 落盘 + 退出（不变）。
- 说明：编辑中仍即时写 sys_cfg，故短按“保存”主要作用是“结束编辑/确认”，闭环效果编辑中已可见；无取消逻辑（用户未要求）。
- 未编译验证，需本机 STM32CubeIDE build 确认。
