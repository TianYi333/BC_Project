# 2026-08-28 工作日志

## 审查：TCP 连接多链路 + 心跳/重连逻辑
- 结论：工程**不存在**对服务端的多余连接链路。
  - 单一全局 `tcp_pcb`，全工程仅 1 处 `tcp_new`/`tcp_connect`（net_comm_task.c:1266/1279，均在 `Tcp_client_task`）。
  - 重连块二次防护 `if(tcp_pcb != NULL){UNLOCK;continue;}`（1261）防重复建链；所有断连路径（safe_tcp_close/tcp_error_callback/tcp_recv_cb错误/tcp_connected_cb失败）均先清空并释放旧 pcb。
  - tcp_send_register/心跳/应答均复用同一 pcb + tcp_send_queue 异步发送，不开第二链路。
- 心跳/重连逻辑问题（3 点，未改代码）：
  1. 【关键耦合】`hb_lost_cnt` 仅在任何服务器有效签名报文验签通过时复位（tcp_parse_cmd:2222），即"链路存活"依赖服务器下发而非 TCP 本身。若服务器纯单向不回包，健康连接约 60s（3×20s，HB_LOST_MAX=2）后被强制断开 → 潜在误杀。需与服务器协议核对；若无回执应改为按"发送成功"复位或启用 lwIP 原生 keepalive。
  2. 【注释过期】HEARTBEAT_PERIOD_MS 注释"30s"实为 20000(20s)；HB_LOST_MAX 注释"累计3次"实为 2；1210 行注释"累计3次"矛盾。
  3. 【边缘】hb_lost_cnt++ 在发送前递增，心跳队列满被丢弃时仍 +1（风险极小）；实际断开在第 3 周期（>=2）触发。
- 重连节流路径正确（g_fast_reconnect + 全局 reconnect_tick + 5s 节流兜底，无重试风暴）。
- 锁序：safe_tcp_close 取 core→tcp_send_mutex；connected_cb 在 tcpip 线程内已持 core 再取 mutex，顺序一致无死锁。
- 未改动代码，仅审查汇报。

## 排查：初次上电首次进入菜单需长按两次
- 现象：设备初次上电后，第一次"进入菜单页面"需要长按两次。
- 定位根因：`oled_task.c:76` 上电后**无条件**调用 `home_page_callback()`（oled_page.c:2666）。该函数显示温湿度页(Home Page)后 `while(!long_pressed)` **阻塞**等待 `ENCODER_PUTH_LONG` 才返回。
  - 因此上电后设备先停在"温湿度首页"，用户须**长按一次**退出 Home Page，主循环(oled_task.c:93)才显示主菜单列表。
  - "两次"推测 = ①长按退出 Home Page 进主菜单；②再在顶层选某项进入二级菜单（顶层长按=BACK 无效，需短按 ENTER），被用户统归为"进入菜单要按两次"。
- 超时回首页路径(oled_task.c:89)同样调 `home_page_callback()` 阻塞等长按，机制一致。
- 未改代码；修复方向取决于设计意图（上电默认显示主菜单列表 vs 温湿度首页），待用户确认。

## 修正：上电首次"长按两次"的根因（替代前述"退出Home Page+进二级菜单"的推测）
- 经查 `Encoder.c:36-38`：`static uint8_t lastA=0; lastB=0; lastP=0;` 初始值与实际空闲电平不符。
- `gpio.c:122-126` `EC11_P_Pin` = `GPIO_MODE_IT_RISING_FALLING` + `GPIO_NOPULL`（无内部上下拉，依赖外部上拉→空闲高=1）。
  - 故 `lastP=0` 与空闲高(1)不一致。上电后第一次按钮按下（下降沿 currentP=0, lastP=0）：`lastP==1 && currentP==0` 不成立 → **漏检，不记录按下**；第一次松开也无事件发出 → `home_page_callback`(只认 PUSH_LONG) 不退出，仍停温湿度页。
  - 第二次按下时 lastP 已被第一次松开同步为1 → 正常识别 → 长按释放发 PUSH_LONG → 退出 Home Page 进主菜单。正好对应"初次上电第一次需长按两次"。
  - 即便外部为下拉(空闲低)，`lastP=0` 虽与空闲一致，但按下(上升沿)被代码误判为"释放"分支，`button_pressed` 不置位 → 首次操作同样异常。故根因是 `lastA/lastB/lastP` 初始未与实际引脚电平同步。
- 修复方向（已实施）：把 `lastA/lastB/lastP` 提升为文件级 static，新增 `Encoder_SyncInitialLevel()` 读取三引脚当前实际电平并置 `g_encoder_synced=1`；`OLED_Task_Entry`（oled_task.c:76）进入 `home_page_callback()` 前调用一次；回调开头 `if(g_encoder_synced==0)` 兜底同步并 `return` 丢弃首中断。因启动期已先同步，`g_encoder_synced` 在用户操作前即为 1，兜底 `return` 不会丢弃真实按键。
- 旁注：`QUEUE_KEY` 深度=1（`msg_queue.c:20`，`xQueueCreate(1,...)`，`xQueueSendFromISR` 满则丢事件），但与本"两次"现象无直接因果。

## 已实施修复（用户确认后）：上电首次长按两次
- 改动文件：
  - `UserCode/Drivers/Encoder/Encoder.c`：lastA/lastB/lastP 由回调内 static 局部 → 文件级 static；新增 `g_encoder_synced` 与 `Encoder_SyncInitialLevel()`；回调首行加首次同步兜底（同步后 return 丢弃该中断）。
  - `UserCode/Drivers/Encoder/Encoder.h`：声明 `Encoder_SyncInitialLevel(void)`。
  - `UserCode/Logic/oled_task.c`：第 76 行 `home_page_callback()` 前新增 `Encoder_SyncInitialLevel();`。
- 预期效果：上电后第一次长按即可一次退出温湿度首页进主菜单，不再需要按两次。
- 验证：本沙箱无 ninja，未编译；需硬件烧录验证。若首电首按仍异常，按用户要求回退三处改动（恢复 Encoder.c 原 36-38 行 static 局部 + 删同步函数/调用 + 删 Encoder.h 声明 + 删 oled_task.c 调用行）。

## OTA 远程升级方案设计（进行中）
- 用户诉求：通过 TCP（TFTP）远程升级固件。
- 已确认三项选择（2026-08-28）：① Boot 策略 = Bootloader + SPI Flash 暂存 + A/B 回滚；② 传输 = lwIP TFTP（设备作 TFTP Server，主机 push 固件；工程已有 tftp_server.c/.h，仅缺 Client）；③ 安全 = 仅 CRC32（不 HMAC 签名，仅适用于可信内网）。
- 可复用资产：TCP JSON 入口 tcp_parse_cmd(2008) + HMAC 体系（仅做授权触发）、SPI Flash(Fal/FlashDB/SFUD)、reboot_system()、fdb_calc_crc32、链接脚本已有 FLASH2(0x08100000,512K)（本方案用 SPI 暂存，不依赖双 bank）。
- 架构：控制通道(TCP JSON upgrade_start{filename,size,crc32} 验签) 仅做授权+回报；传输通道(TFTP UDP69) 流式写 SPI firmware_a 边收边算 CRC32；close 时比对 → ota_meta(pending) → TCP 回报 → reboot_system；Bootloader 备份 App→firmware_bak，拷 firmware_a→内部Flash，校验，跳 App，失败回滚。
- 用户追问澄清：Bootloader 是否独立工程？结论——**不需要独立 CubeMX 工程**，推荐"单 CMake 工程 + 两个构建 target（boot/app）"：共用源码树(Fal/SFUD/SPI/CRC)，各自 main + 链接脚本(boot.ld/app.ld)，不新增 .ioc。物理 Flash 低端划 Bootloader 区(0x08000000 +64KB)，App 移到 0x08010000(+448KB)，各自 VTOR。
- 烧录顺序：**首次**先 SWD 烧 boot.bin(一次性) 再烧 app.bin；**日常** TFTP OTA 只更新 App 区，不动 boot 区；Bootloader 自身升级极少，需 SWD 或专用流程。
- 待用户最终确认"单工程两 target"组织方式后分步实现（每步给核对）：lwipopts.h 开 LWIP_TFTP、fal_cfg.h 加三分区、FLASH.ld 划 boot/app、新增 boot target+main_boot.c、新增 ota.c/h(TFTP回调+upgrade_start命令挂 tcp_parse_cmd)、net_comm_task.c 加命令分发。
- 风险：首次烧 Bootloader 有变砖风险，需保留 SWD/JTAG 救砖；双镜像构建/合并需规划。
- 【第1步受阻·已解决】原发现 `lwipopts.h` 未开 `LWIP_TFTP` 且缺 `tftp_server.c` 实现。用户已通过 CubeMX 重勾选 **LWIP_TFTP=1** 并重新生成，验证现状如下：
  - `LWIP/Target/lwipopts.h:166,168` 已有 `#define LWIP_TFTP 1`（**出现两次**，值相同，是 CubeMX 重复生成产物，无害但建议清理其一）。
  - `Middlewares/Third_Party/LwIP/src/apps/tftp/tftp_server.c` **已存在**（实现就位）。
  - `cmake/stm32cubemx/CMakeLists.txt:200` 已把 `tftp_server.c` 纳入 `LwIP_Src` 编译。
  - `tftp_server.h` API：`err_t tftp_init(const struct tftp_context* ctx)`、`void tftp_cleanup(void)`；`struct tftp_context` 含 `open/write/close/read` 回调（write-only OTA 只需 open/write/close）。TFTP 端口默认 69(UDP)，`TFTP_MAX_FILENAME_LEN=20`。
  - 全局 grep 确认：**工程业务代码无任何人调用过 `tftp_init`** → 后续 OTA 模块调用不会重复初始化冲突。
  - 结论：第1步（启用官方 TFTP Server 并纳入编译）**已完成**，无需自写 TFTP Server（原方案 A 废弃）。注意 `tftp_context.write` 回调在 LwIP tcpip 线程上下文执行，OTA 写入 SPI Flash 时需注意不要长时间阻塞 tcpip 线程（建议写回调里只搬数据/累计 CRC32，扇区擦除/大块写可缓冲或异步）。
- 【第2步·已完成】`fal_cfg.h` 的 `FAL_PART_TABLE` 新增 3 个 OTA 分区（追加在 ef_kvdb3 之后，全部 64KB 对齐、落在 8MB 内，与 KV 三分区[0–192KB]无重叠）：
  - `firmware_a`：offset 192KB(0x30000)，size 1024KB(0x100000) —— 暂存新固件镜像（TFTP 边收边写）。
  - `firmware_bak`：offset 1216KB(0x130000)，size 1024KB —— 已知良好 App 备份（Bootloader 回滚用）。
  - `ota_meta`：offset 2240KB(0x230000)，size 64KB —— OTA 状态机/CRC/长度等元数据（裸分区，Bootloader/OTA 用 `fal_partition_read/write/erase` 直接操作，不挂 FlashDB）。
  - 合计用到 2304KB(2.25MB) < 8MB(W25Q64)，留足余量；App 现约 320KB，每镜像给 1MB 可抗增长。
- 【第3步·已实施（用户确认"App 放 0x08100000 FLASH2 / Bootloader 64KB"）】内部 Flash 双区 + 单工程双 target 改造：
  - **App 链接脚本** `STM32H743XG_FLASH.ld`：`FLASH` ORIGIN 由 `0x08000000` 改为 `0x08100000`(FLASH2)，删原 `FLASH2` 区域；App 现占第二 bank 512KB（余量 147KB，比挤在 0x08010000+448KB 更稳）。
  - **App 设 VTOR**：`Core/Src/main.c` `main()` 开头 `USER CODE BEGIN 1` 加 `SCB->VTOR = 0x08100000UL;`（Bootloader 区默认 VTOR=0 指向 0x08000000，App 须显式改，否则中断跳错区 HardFault）。未改 `system_stm32h7xx.c`（VECT_TAB 默认不设置，更不易被 CubeMX 重新生成冲掉）。
  - **新 boot.ld**：`FLASH ORIGIN=0x08000000, LENGTH=64K`；保留 RAM_D1/D2/D3/DTCM/ITCM 区域与标准段(.isr_vector/.text/.rodata/.ARM.*/.data/.bss/._user_heap_stack)；不含 ETH/DMA/LetterShell 特殊段（boot 不用）。
  - **新 main_boot.c**（最小跳转壳，第4步再加 SPI 烧录/回滚）：`HAL_Init()`(默认 HSI) → 校验 App 向量表有效性(MSP 在 D1 SRAM 0x24000000~0x24100000、Reset_Handler 在 0x08100000~0x08200000) → `JumpToApp()`（关中断+`__set_MSP`+`SCB->VTOR=0x08100000`+跳 Reset_Handler）；无效则死循环保留 SWD 救砖。
  - **CMakeLists.txt** 新增 `boot` target：`startup_stm32h743xx.s` + `Core/Src/main_boot.c`；`target_link_libraries(boot STM32_Drivers stm32cubemx ${TOOLCHAIN_LINK_LIBRARIES})`（复用 HAL 对象库+包含路径/宏，未引用代码被 --gc-sections 丢弃）；`target_link_options(boot PRIVATE -T boot.ld)`（链接器取最后一个 -T 覆盖全局 FLASH.ld）；POST_BUILD 生成 `boot.hex`/`boot.bin`。
  - **验证限制**：本沙箱无 ninja，未编译。需在你本地 CMake 重新 configure + 构建 `Oil_Pump_Control` 与 `boot` 两 target，确认：① 两 target 均链接成功；② `boot.bin < 64KB`；③ App 链接地址确在 0x08100000（看 map/elf 头）。
  - **回退预案**：若双 target 构建异常，回退 = 恢复 `STM32H743XG_FLASH.ld` 原 FLASH/FLASH2 两行 + 删除 main.c 的 VTOR 两行 + 删除 `boot.ld`/`main_boot.c` + 删除 CMakeLists 的 boot target 整段。
- 【下一步·第4步】在 `main_boot.c` 增加 OTA 烧录/回滚：读 `ota_meta` → 若 pending 则校验 `firmware_a` 的 CRC32/长度 → 备份当前 App 到 `firmware_bak` → 把 `firmware_a` 拷入内部 Flash App 区(0x08100000) → 置 `ota_meta=ok` → `JumpToApp()`；新固件起不来则用 `firmware_bak` 回滚。需 Fal/SFUD 初始化（复用 App 的初始化代码或搬最小子集） + 内部 Flash 编程(HAL_FLASH_Unlock/Program) + IWDG 防卡死。
